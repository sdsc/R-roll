<?xml version="1.0" standalone="no"?>

<kickstart>

<description>
The R Project for Statistical Computing module files
http://www.r-project.org
</description>

<copyright>
Copyright (c) 2000 - 2009 The Regents of the University of California.
All rights reserved. Rocks(r) v5.1 www.rocksclusters.org
</copyright>

<changelog>
</changelog>

<post>

# Remove /etc/profile.d files in favor of loadable modules when available
if test -f /etc/profile.d/R.sh; then

  /bin/rm -f /etc/profile.d/R.*

  if test `grep -c /opt/modulefiles/applications /etc/profile.d/modules.csh` = 0; then
    echo 'setenv MODULEPATH /opt/modulefiles/applications:$MODULEPATH' >> \
         /etc/profile.d/modules.csh
  fi
  if test `grep -c /opt/modulefiles/applications /etc/profile.d/modules.sh` = 0; then
    echo 'export MODULEPATH=/opt/modulefiles/applications:$MODULEPATH' >> \
         /etc/profile.d/modules.sh
  fi

  /bin/mkdir -m 0755 -p /opt/modulefiles/applications/R

<file name="/opt/modulefiles/applications/R/2.11.1" perms="0644">
#%Module1.0
prepend-path PATH /opt/R/bin
prepend-path R_LIBS /opt/R/local/lib
</file>

<file name="/opt/modulefiles/applications/R/.version.2.11.1">
#%Module1.0
set ModulesVersion "2.11.1"
</file>

  /bin/ln -s /opt/modulefiles/applications/R/.version.2.11.1 \
             /opt/modulefiles/applications/R/.version

fi

</post>

</kickstart>
